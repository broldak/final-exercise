import activity from '../data/activity';

/* Reducers */

const initialState = {
  activities: activity
};

function vizApp(state = initialState, action) {
  // TODO: handle actions
  return state;
}

export default vizApp;
